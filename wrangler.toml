# Cloudflare Pages 配置，开启 nodejs_compat 以兼容 Next 14 生成的 Worker 依赖。
name = "mola-link"
# Next.js on Pages 默认输出
pages_build_output_dir = ".vercel/output/static"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]
